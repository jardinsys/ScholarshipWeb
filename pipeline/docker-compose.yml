services:
  crawler:
    build:
      context: .
      dockerfile: Dockerfile.crawler
    container_name: crawler
    depends_on:
      - mongo
    volumes:
      - .:/app

  mongo:
    image: mongo:latest
    container_name: crawler-mongo
    ports:
      - "27017:27017"
